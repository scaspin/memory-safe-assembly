use std::process::Command;

fn main() {
    //let res = Command::new("gcc")
    //    .args(&["-c", "shaimath.s", "-o", "math.o"])
    //    .output()
    //    .expect("Failed to compile assembly code");
    println!("Assembly code compiled successfully!");
}
